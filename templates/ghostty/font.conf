# Generated by xghost. Do not edit.
# Run 'xghost settings set KNOB_FONT <family>' to change the font.
#
# The font family of the desktop, in the form Ghostty reads. The prescribed
# Ghostty configuration includes this file, and an included file is applied
# after the file that includes it, so this value wins.
#
# 'font-family' is the one setting the knob carries. Ghostty appends every
# 'font-family' it reads to one list, and the first of them is the family it
# draws with, so the prescribed file names none: two lines would leave the
# prescribed family drawing the terminal and the knob doing nothing.

font-family = @KNOB_FONT@

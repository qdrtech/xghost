# Generated by xghost. Do not edit.
# Run 'xghost settings set KNOB_ANIMATIONS on' to switch the animations on.
#
# This fragment is the one for 'KNOB_ANIMATIONS=off'.
#
# It carries no bezier and no animation line. 'enabled = false' switches every
# animation off at once, so a curve that nothing plays would be text that says
# something the compositor never does.
animations {
    enabled = false
}

name: ci

# A pull request branch lives in this repository, so an unfiltered 'push'
# trigger would run every pull request twice. Push covers the default branch.
on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

# A new push to the same branch cancels the run of the previous one.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: dispatcher tests
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      # actions/checkout 4.2.2, pinned to the commit that carries the v4.2.2
      # tag. A tag moves; a commit does not.
      - name: Check out the repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      # bats-core 1.14.0, pinned to the commit that carries the v1.14.0 tag.
      # The same version is packaged as 'bats' in the Arch 'extra' repository.
      - name: Install bats-core
        env:
          BATS_COMMIT: eb7f42f8d608ac693d7a4b67474f6714ea68cfc5
        run: |
          set -euo pipefail
          source_dir=$RUNNER_TEMP/bats-core
          mkdir -p "$source_dir"
          git -C "$source_dir" init --quiet
          git -C "$source_dir" remote add origin https://github.com/bats-core/bats-core.git
          git -C "$source_dir" fetch --depth 1 origin "$BATS_COMMIT"
          git -C "$source_dir" checkout --quiet FETCH_HEAD
          "$source_dir/install.sh" "$RUNNER_TEMP/bats"
          echo "$RUNNER_TEMP/bats/bin" >>"$GITHUB_PATH"

      - name: Report the bats version
        run: bats --version

      # The GTK4 bindings for Python. tests/swaync.bats parses the style sheet
      # of the notification centre with a real Gtk.CssProvider, and that
      # measurement is the evidence base of the SwayNC bundle: it is what says
      # the relative '@import' resolves through the bridge, and what says GTK
      # reports a broken one exactly once. Without these packages both tests
      # skip, and a skip and a pass read the same in a summary line.
      #
      # 'gir1.2-gtk-4.0' carries the typelib that 'gi.require_version("Gtk",
      # "4.0")' loads; 'python3-gi' carries the binding itself. Neither is a
      # dependency of xghost, so no manifest of the project declares them: a
      # GTK4 is on every machine this desktop installs on, because the swaync
      # package depends on it, and the Python binding is a thing only this
      # suite needs.
      #
      # 'python3-jsonschema' is for the check that reads the prescribed
      # configuration against the schema the swaync package ships. That schema
      # is not on this runner, so the check still skips here; the package is
      # installed so the skip has one cause rather than two.
      - name: Install the GTK4 bindings for Python
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install --no-install-recommends --yes \
            python3-gi gir1.2-gtk-4.0 python3-jsonschema

      - name: Report that the GTK4 bindings are there
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import gi
          gi.require_version("Gtk", "4.0")
          from gi.repository import Gtk
          print("GTK %d.%d.%d" % (
              Gtk.get_major_version(),
              Gtk.get_minor_version(),
              Gtk.get_micro_version(),
          ))
          PY

      # The fixture directory holds command files today, so this step proves
      # the lint pass against real content.
      - name: Lint the fixture command metadata
        env:
          XGHOST_COMMAND_DIR: tests/fixtures/commands
        run: ./bin/xghost --lint

      - name: Lint the shipped command metadata
        run: |
          set -uo pipefail
          status=0
          ./bin/xghost --lint || status=$?
          # Exit code 3 means the directory holds no command file. The first
          # shipped command arrives in a later slice, so 3 is the expected
          # state of commands/ today. Any other non-zero code is a failure.
          if [ "$status" -ne 0 ] && [ "$status" -ne 3 ]; then
            exit "$status"
          fi

      # XGHOST_REQUIRE_GTK4 turns the GTK4 skip into a failure. This runner
      # installed the bindings above, so a skip here means the install step
      # stopped working and the measurement quietly stopped running.
      - name: Run the test suite
        env:
          XGHOST_REQUIRE_GTK4: "1"
        run: bats --print-output-on-failure tests

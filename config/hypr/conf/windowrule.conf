# The rules for a window.
#
# Prescribed configuration. See docs/adr/0001-prescribed-config-architecture.md.
#
# There is no rule today. The dotfiles this bundle comes from carried a list of
# rules with every line commented out, so not one of them reached the
# compositor. A rule this desktop needs is added here as a rule rather than as
# an example.

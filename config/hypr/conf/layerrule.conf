# The rules for a layer surface, such as the bar or the notification centre.
#
# Prescribed configuration. See docs/adr/0001-prescribed-config-architecture.md.
#
# There is no rule today, and the bar is here. The dotfiles this desktop comes
# from carried three rules for the bar and three for the notification centre,
# and every one of them was commented out. All six blur a surface, and
# conf/decoration.conf switches the blur of this compositor off, so a rule that
# named the bar would say nothing. docs/bundles/waybar.md records the bar.
#
# The notification centre arrives with issue #14 and brings the rules its
# surface needs.
